# scalar-config

Scalar platform settings registry (schema `scalar_config`). Spec: [docs/05-microservices/scalar-config/README.md](../../docs/05-microservices/scalar-config/README.md).

```bash
pnpm exec turbo run dev --filter=@tavrida/scalar-config
```

Port **3008** (`SCALAR_CONFIG_PORT`).
