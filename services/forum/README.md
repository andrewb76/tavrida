# forum

Forum microservice (schema `forum`). Spec: [docs/05-microservices/forum/README.md](../../docs/05-microservices/forum/README.md).

```bash
pnpm exec turbo run dev --filter=@tavrida/forum
```

Port **3009** (`FORUM_PORT` / `PORT`).
