# Traefik dynamic config

Файлы здесь подхватываются file provider (`providers.file.directory` в `traefik.dev.yml`).

Примеры: HTTP middlewares, TLS options, rate limits. Роутеры и сервисы — через Swarm labels на сервисах stack-файлов.
