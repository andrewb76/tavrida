{
  "name": "auction-platform-monorepo",
  "private": true,
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@9.1.0",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "test:bdd": "turbo test:bdd"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
