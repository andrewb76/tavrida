Agent backlog (Tavrida Lot):
  Общий файл задач для AI + Todo+ sidebar. Правило: .cursor/rules/agent-tasks.mdc
  План дней (неделя: renew CRON + invitation.redeemed): docs/00-meta/WORK-PLAN-NEXT.md

Invites / BFF:
  ✔ OpenAPI fragment Vanga в docs/06-api/vanga-admin-api.md @vanga @docs @done(26-07-11)

Vanga (прогноз дохода):
  ☐ Checkpoint 0: founder review topic-index + vanga.defaults.yaml @vanga
  ✔ packages/monetization-engine — MRR/costs fixes + golden tests @vanga @done(26-07-11)
  ✔ BFF GET/POST /admin/vanga/* @vanga @bff @done(26-07-11)
  ✔ Frontend /admin/vanga — вкладки по topic-index @vanga @frontend @done(26-07-11)
  ☐ services/vanga :3013 — saved scenarios (фаза 3) @vanga
  ☐ Экспорт CSV/PDF @vanga @docs
  ☐ Monte Carlo @vanga
  ☐ settings domain vanga.* @vanga @settings

plan-config:
  ✔ services/plan-config v1 — plans, limits, subscription scaffold @plan-config @done(26-07-11)
  ✔ Internal renew/run + billing charge + expire fail path @plan-config @billing @done(26-07-16)
  ✔ Swarm: plan-config-renew hourly → renew/run @plan-config @ops @done(26-07-16)
  ☐ Team review: tariff Seller/Buyer/Pro proposal @plan-config @docs
  ☐ После консенсуса — каскад docs + seed (basic → seller/buyer) @plan-config @docs
  ☐ Post-MVP: три реестра (scalar · plan · user-prefs) — сервис + audit PLATFORM-REGISTRY + миграция DeliveryPreference/webhook prefs @plan-config @scalar @docs
    см. ADR-020; MVP не трогаем

Subscriptions (event intent):
  ✔ Rename auction-subscriptions → subscriptions + CRUD v1 + BFF @subscriptions @done(26-07-13)
  ✔ Seed plan-config subscriptions.member.* keys @plan-config @subscriptions @done(26-07-13)
  ✔ Frontend subscribe UI (lot/topic) @frontend @subscriptions @done(26-07-13)
  ✔ DeliveryPreference + digest CRON stub @subscriptions @done(26-07-13)
  ✔ Internal match endpoint @subscriptions @done(26-07-13)
  ✔ Forum Formal Tag/ContentTag + UI subscribe on tag chip @forum @subscriptions @frontend @done(26-07-15)
  ✔ Publish tag.content_tagged (BFF addedTagIds → match → notifications) @forum @subscriptions @bff @done(26-07-15)
  ✔ Scaffold services/notifications + trigger/mock + tag-content workflow id @notifications @done(26-07-15)
  ✔ Richer subscriptions UI (list + delivery prefs) @frontend @subscriptions @done(26-07-15)
  ✔ Local compose Novu CE + ADR-019 + pnpm novu:up @notifications @done(26-07-16)
  ☐ Novu ops: задокументировать onboarding Dashboard + API key + workflow tag-content (отложено) @notifications @docs
  ☐ Novu: настроить local (ключ + tag-content) после чеклиста @notifications
  ☐ После проверки local Novu — merge в infra.local.yml (как Keto) @notifications @ops

Forum:
  ✔ Topic drafts DRAFT/PUBLISHED (author-only, no unpublish) @forum @frontend @docs @done(26-07-21)

Chat:
  ✔ requirements/analysis.md + registry keys + ADR-020 prefs backlog @chat @docs @done(26-07-22)
  ✔ README MICROSERVICE-SPEC + chat-api + event-catalog + username spec @chat @docs @done(26-07-22)
  ✔ Scaffold services/chat :3016 (entities, migration, internal ensure/list/messages) @chat @done(26-07-22)
  ✔ pnpm install — @tavrida/chat in workspace, build+test ok @chat @ops @done(26-07-22)
  ✔ BFF chats module (list/unread/self/direct/topic/messages + plan gates) @chat @bff @done(26-07-22)
  ✔ GROUP spawn/invite/leave + plan gates + spawn UI @chat @bff @frontend @done(26-07-22)
  ✔ RMQ TOPIC consumers (forum.topic_published / comment_created) @chat @forum @done(26-07-22)
  ✔ plan/scalar seed+sync chat keys + admin UI sections @chat @plan-config @done(26-07-22)
  ☐ Keto chat:{id}#member (optional hardening) @chat @bff
  ✔ Frontend «Мои чаты» + TOPIC bottom sheet @chat @frontend @done(26-07-22)
  ✔ DM displayTitle (peer name) + message DELIVERED/READ (+ FE SENDING) @chat @bff @frontend @docs @done(26-07-22)
  ✔ Username UNIQUE + search API + chat @mention autocomplete @user-profile @chat @bff @frontend @docs @done(26-07-22)
  ✔ Logto Console: username policy + Email/Username sign-up + Account Center Edit @ops @logto @done(26-07-22)
  ☐ Verify: set/change username → webhook → user_profile.username; autocomplete search @user-profile @logto
  ☐ Backfill: pnpm sync:logto-users on Swarm if stale usernames @user-profile @ops
  ✔ user-profile: UNIQUE(lower(username)) + search for @mention @user-profile @chat @done(26-07-22)

Auction:
  ✔ Catalog list/get/create + seed + FE @auction @done(26-07-16)
  ✔ English bid + close/run + RMQ + FE placeBid @auction @bff @frontend @done(26-07-16)
  ✔ Dutch accept bid + ask step-down in close/run + FE CTA @auction @frontend @done(26-07-16)
  ☐ promote billing charge · expert POST · WS live @auction

Periods (historical reference):
  ✔ Spec + Nest scaffold categories/periods + partition rules @periods @done(26-07-13)
  ✔ BFF public/admin + admin UI (schema + CRUD + partition) @periods @frontend @bff @done(26-07-13)
  ✔ Seed данных Крыма / Причерноморья @periods @done(26-07-14)
  ☐ d3.js SVG timeline widget (выбор / ограничение периода) @frontend @periods
  ☐ Привязка auction/forum → periodId @auction @forum @periods

Marketplace:
  ✔ Scaffold listings/orders + BFF + member UI @marketplace @done(26-07-14)
  ✔ Portfolio upload via media + seed demo listings @marketplace @frontend @done(26-07-14)
  ✔ Orders UI (provider/customer) @marketplace @frontend @done(26-07-14)
  ✔ RMQ order_completed → deal-feedback pending @marketplace @feedback @done(26-07-14)

Forum votes (+/-):
  ✔ content_vote + changeWindowMinutes + UI pressed state @forum @frontend @done(26-07-14)
  ✔ При +/- голосе: Δkarma автору + log FORUM_VOTE @forum @user-profile @done(26-07-14)

Reputation log:
  ✔ Click rating/karma → popup log (user-profile reputation_change_log) @frontend @user-profile @done(26-07-14)
  ✔ При deal-feedback submit: Δrating + log (source=DEAL_FEEDBACK) @feedback @user-profile @done(26-07-14)

Testing rollout:
  ☐ T0: test discovery, zero-test guard, scripts, coverage baseline, parallel CI @testing @ci
  ☐ T1: Testcontainers PostgreSQL — migrations, transactions, concurrency @testing @backend
  ☐ T2: Nest HTTP/auth + OpenAPI/event contract gates @testing @security @bff
  ☐ T3: RabbitMQ/outbox retry, DLQ, recovery, idempotency integration @testing @rabbitmq
  ☐ T4: Vitest Vue component/store/router + MSW + axe @testing @frontend
  ☐ T5: Playwright critical journeys + visual/accessibility smoke @testing @frontend
  ☐ T6: deploy gates, k6, ZAP, image scan, release rehearsals @testing @ops

Dev Swarm (evatorg.su):
  ✔ Репо: CD только с dev + apex redirect + docs @ops @docs @done(26-07-18)
  ✔ Logto OSS в stack-infra (auth./logto.) вместо Cloud @ops @done(26-07-19)
  ☐ DNS A: auth + logto (+ остальные) → 193.142.148.175 @ops
  ☐ Logto Console: SPA + API resource + M2M; GH env VITE_LOGTO_* @ops
  ☐ Deploy с rebuild frontend + приёмка login/audience @ops
  ☐ Защита ветки dev @ops

Docs / workflow:
  ✔ Добавить CLUB_INVITES_* в PLATFORM-SECRETS.md @done(26-07-11)
  ✔ ops-hygiene.md — регулярная рутина infra/docs/cron @docs @done(26-07-16)
  ☐ Visual Lab W03: выбрать направление A/B/C → tokens pilot @docs @frontend

Review remediation:

Archive:
  ✔ Review P2: legacy naming/API/status documentation audit @docs @done(26-07-17)
  ✔ Review P1: frontend identity/route/filter stale state races @frontend @done(26-07-17)
  ✔ Review P1: fail-closed plan enforcement для paid/limited writes @plan-config @bff @done(26-07-17)
  ✔ Review P1: transactional outbox + retry/DLQ для domain events @backend @rabbitmq @done(26-07-17)
  ✔ Review P1: production service-token middleware для всех internal HTTP endpoints @security @backend @done(26-07-17)
  ✔ Review P0: BFF JWT fail-closed + explicit local dev-token @security @bff @done(26-07-17)
  ✔ Review P0: hydrated DATABASE_URL используется TypeORM во всех сервисах @backend @ops @done(26-07-17)
  ✔ Review P0: versioned baseline migrations для 12 PostgreSQL schemas @backend @ops @done(26-07-17)
  ✔ Review P0: transactional auction bid/close + cron advisory lock @auction @done(26-07-17)
  ✔ Dutch accept bid + ask step-down in close/run + FE CTA @auction @frontend @done(26-07-16)
  ✔ plan-config HTTP limits (replace BFF hardcoded policy) @auction @bff @done(26-07-16)
  ✔ Swarm cron: POST …/auctions/close/run @auction @ops @done(26-07-16)
  ✔ Health /ready DB ping @auction @done(26-07-16)
  ✔ Auction English bid + close/run + RMQ events + FE placeBid @auction @bff @frontend @done(26-07-16)
  ✔ Emit invitation.redeemed (RMQ) after claim @user-profile @done(26-07-16)
  ✔ Internal renew/run + billing charge + expire fail path @plan-config @billing @done(26-07-16)
  ✔ E2E: create → resolve → mock signIn → claim @bff @done(26-07-16)
  ✔ Интеграция club.member.invite.monthlyMax через plan-config @bff @plan-config @done(26-07-16)
  ✔ Shared subscription store для tag chips (N× GET /subscriptions) @frontend @subscriptions @done(26-07-16)
  ✔ OpenAPI fragment для invites в docs/06-api/invites-api.md @bff @docs @done(26-07-16)
  ✔ BFF POST /plans/activate + billing charge (price>0) @plan-config @billing @done(26-07-16)
  ✔ RMQ consumers → match → notifications @subscriptions @notifications @done(26-07-16)
  ✔ Enrich subscription list titles (topic/tag/auction names via BFF) @frontend @subscriptions @done(26-07-16)
  ✔ Admin impersonation (X-Act-As + banner + Войти как) @bff @frontend @security @done(26-07-16)
  ✔ Настроить AGENT-DOCS-INDEX + правило docs-workflow @done(26-07-10)
  ✔ Доделать BFF invites: валидация, 201, admin skip limit @done(26-07-10)
  ✔ Invites: validityDays + codeType из settings (ClubSettingsReader) @done(26-07-11)
  ✔ inviteOnly: GET /settings/public + landing/join UI @done(26-07-11)
  ✔ Vanga spec: monetization-catalog + ADR-014 + vanga README @done(26-07-11)
  ✔ Vanga config: vanga.defaults.yaml + vanga-config.md (решения founder) @done(26-07-11)
  ✔ Vanga docs tree: glossary, overview, topic-index, topics/*, IMPLEMENTATION-PLAN @done(26-07-11)
