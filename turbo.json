{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "concurrency": "20",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".vite/**", "node_modules/.vite/**", ".vitepress/dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["^build"]
    },
    "lint": {
      "outputs": [],
      "cache": false
    },
    "test": {
      "dependsOn": ["^build", "build"],
      "outputs": [],
      "cache": false
    },
    "generate": {
      "outputs": ["packages/graphql/src/generated/**"]
    }
  }
}
